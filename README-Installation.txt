RFID Laboratory Monitor - Installation Instructions
=================================================

🎯 QUICK START:
1. Run "RFID NDC123 Client Setup.exe" as Administrator
2. Right-click RFID icon in system tray → Settings
3. Enter server IP address and click Save
4. Done! The application runs automatically in background

📋 DETAILED INSTALLATION:

1. INSTALLATION:
   - Right-click "RFID NDC123 Client Setup.exe"
   - Select "Run as administrator"
   - Follow the installation wizard
   - Choose installation directory (default is recommended)
   - The application will start automatically in system tray

2. FIRST-TIME CONFIGURATION:
   - Look for RFID icon in system tray (bottom-right corner of screen)
   - Right-click the RFID icon
   - Select "Settings" from the menu
   - Enter the server IP address: [YOUR_SERVER_IP_HERE]
   - Set WebSocket port: 8080 (default)
   - Click "Save Settings"
   - Click "Restart Logger" if needed

3. VERIFICATION:
   - Right-click tray icon → "View Status"
   - Verify "Logger Status: Running"
   - Check "Server Address" shows your server IP
   - Verify "Connection Status: Connected"

4. AUTOMATIC UPDATES:
   - Updates are downloaded automatically in background
   - You'll receive a notification when updates are ready
   - Right-click tray icon → "Check for Updates" for manual check
   - Click "Restart to Update" when update is ready

5. DAILY USAGE:
   - The application runs silently in system tray
   - No visible windows or interfaces
   - Right-click tray icon to access:
     * Settings (configure server)
     * View Status (check system health)
     * Check for Updates (manual update check)
     * Licensing & Documentation (help and support)

6. TROUBLESHOOTING:
   - If tray icon is not visible: Check system tray settings
   - If connection fails: Verify server IP address in Settings
   - If no data collection: Check "View Status" for errors
   - For more help: Right-click tray icon → "Licensing & Documentation"

7. SUPPORT:
   - System Administrator: [YOUR_CONTACT_INFO]
   - IT Support: [IT_CONTACT_INFO]
   - Project Lead: [LEAD_CONTACT_INFO]
   - Email: [SUPPORT_EMAIL]

📊 SYSTEM REQUIREMENTS:
- Windows 10/11 (any edition)
- 4GB RAM minimum
- 50MB free disk space
- Network connection to server
- Administrator privileges for installation

🔒 PRIVACY & SECURITY:
- Application monitors PC usage for research purposes
- Data is transmitted securely to central server
- No personal information is collected
- All data collection is for laboratory monitoring only

⚠️ IMPORTANT NOTES:
- Always run installer as Administrator
- Ensure stable network connection to server
- Keep application updated for best performance
- Contact support if you experience any issues

🎉 INSTALLATION COMPLETE!
Your RFID monitoring client is now running in the system tray.
The application will automatically start with Windows and run in the background.
